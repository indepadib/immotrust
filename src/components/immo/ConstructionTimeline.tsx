import React from 'react'; import { HardHat, CheckCircle2, Circle, Clock, AlertCircle } from 'lucide-react'; import { clsx } from 'clsx'; interface Milestone { label: string; status: 'completed' | 'ongoing' | 'upcoming' | 'delayed'; date: string; } interface ConstructionTimelineProps { milestones: Milestone[]; } export const ConstructionTimeline = ({ milestones }: ConstructionTimelineProps) => { return ( <div className="bg-white rounded-[3rem] p-12 border border-slate-100 shadow-2xl"> <div className="flex items-center gap-4 mb-12"> <div className="p-3 bg-primary/10 rounded-2xl text-primary"> <HardHat className="w-8 h-8" /> </div> <div> <h3 className="text-2xl font-black text-secondary uppercase italic tracking-tight">Suivi de Chantier</h3> <p className="text-[10px] font-bold text-slate-400 uppercase tracking-widest">Dernière mise à jour : Hier par Expert Audit</p> </div> </div> <div className="relative space-y-12"> {/* Connection Line */} <div className="absolute left-[19px] top-4 bottom-4 w-0.5 bg-slate-100 " /> {milestones.map((ms, idx) => ( <div key={idx} className="relative flex items-start gap-8 group"> <div className={clsx( "z-10 w-10 h-10 rounded-full flex items-center justify-center border-4 border-[#fafafa] transition-all", ms.status === 'completed' ? "bg-emerald-500 text-white" : ms.status === 'ongoing' ? "bg-primary text-secondary ring-4 ring-primary/20 animate-pulse" : ms.status === 'delayed' ? "bg-rose-500 text-white" : "bg-slate-200 text-slate-400" )}> {ms.status === 'completed' ? <CheckCircle2 className="w-5 h-5" /> : ms.status === 'delayed' ? <AlertCircle className="w-5 h-5" /> : ms.status === 'ongoing' ? <Clock className="w-5 h-5" /> : <Circle className="w-5 h-5" />} </div> <div className={clsx( "flex-1 p-6 rounded-[2rem] border transition-all", ms.status === 'ongoing' ? "bg-primary/5 border-primary/20 shadow-lg shadow-primary/5" : "bg-transparent border-transparent" )}> <div className="flex justify-between items-start mb-2"> <h4 className={clsx( "text-[12px] font-black uppercase tracking-tight", ms.status === 'upcoming' ? "text-slate-400" : "text-secondary " )}>{ms.label}</h4> <span className="text-[9px] font-black text-slate-400 uppercase tracking-widest">{ms.date}</span> </div> {ms.status === 'delayed' && ( <p className="text-[9px] font-bold text-rose-500 uppercase tracking-widest">Retard constaté de 45 jours</p> )} {ms.status === 'ongoing' && ( <div className="mt-4 h-1.5 w-full bg-primary/20 rounded-full overflow-hidden"> <div className="h-full bg-primary w-2/3" /> </div> )} </div> </div> ))} </div> </div> ); }; 