import React from 'react'; import { Shield, TrendingUp, AlertTriangle, CheckCircle, Info, Star } from 'lucide-react'; import { clsx } from 'clsx'; interface ScoreBadgeProps { score: number; label?: string; size?: 'sm' | 'md' | 'lg'; showIcon?: boolean; } export const ScoreBadge = ({ score, label, size = 'md', showIcon = true }: ScoreBadgeProps) => { const getColor = (s: number) => { if (s >= 8) return 'text-score-green bg-score-green/10 border-score-green/20'; if (s >= 6) return 'text-score-orange bg-score-orange/10 border-score-orange/20'; return 'text-score-red bg-score-red/10 border-score-red/20'; }; const getIcon = (s: number) => { if (s >= 8) return <Shield className="w-full h-full" />; if (s >= 6) return <TrendingUp className="w-full h-full" />; return <AlertTriangle className="w-full h-full" />; }; const sizes = { sm: 'p-1.5 gap-2 text-[10px]', md: 'p-3 gap-3 text-xs', lg: 'p-6 gap-4 text-2xl font-black italic shadow-2xl' }; const iconSizes = { sm: 'w-3 h-3', md: 'w-4 h-4', lg: 'w-8 h-8' }; return ( <div className={clsx( "inline-flex items-center rounded-[1.5rem] border font-black uppercase tracking-widest transition-all", getColor(score), sizes[size] )}> {showIcon && <div className={iconSizes[size]}>{getIcon(score)}</div>} <div className="flex flex-col items-start leading-none"> {label && <span className="text-[7px] opacity-60 mb-1">{label}</span>} <span>{score < 10 && score % 1 === 0 ? score.toFixed(1) : score}/10</span> </div> </div> ); }; 