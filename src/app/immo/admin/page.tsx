'use client'; import React, { useState, useEffect } from 'react';
import { supabase } from '@/lib/supabase/client';
import { ProjectService } from '@/lib/immo/ProjectService';
import { DeveloperService } from '@/lib/immo/DeveloperService';
import { SeedService } from '@/lib/immo/SeedService';
import { Plus, Search, Edit2, Trash2, ExternalLink, LayoutDashboard, Settings, LogOut, CheckCircle2, AlertCircle, Image as ImageIcon, Save, X, Building2, MapPin, ShieldCheck, TrendingUp, Activity, Lock, RefreshCcw, Database
} from 'lucide-react';
import Image from 'next/image';
import Link from 'next/link'; // --- AUTH GATE ---
const ADMIN_PASSWORD = process.env.NEXT_PUBLIC_ADMIN_PASSWORD || 'AvisPromoteur2026'; export default function AdminDashboard() { const [isAuthenticated, setIsAuthenticated] = useState(false); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [view, setView] = useState<'projects' | 'developers' | 'reviews'>('projects'); const [items, setItems] = useState<any[]>([]); const [developers, setDevelopers] = useState<any[]>([]); const [loading, setLoading] = useState(true); const [syncing, setSyncing] = useState(false); const [fetchError, setFetchError] = useState<string | null>(null); const [isEditing, setIsEditing] = useState(false); const [editingItem, setEditingItem] = useState<any>(null); useEffect(() => { if (isAuthenticated) { fetchData(); } }, [isAuthenticated, view]); const handleLogin = (e: React.FormEvent) => { e.preventDefault(); if (password === ADMIN_PASSWORD) { setIsAuthenticated(true); setError(''); } else { setError('Mot de passe incorrect'); } }; const fetchData = async () => { setLoading(true); setFetchError(null); try { if (view === 'projects') { const [pData, dData] = await Promise.all([ ProjectService.getAllProjects(), DeveloperService.getAllDevelopers() ]); setItems(pData); setDevelopers(dData); if (pData.length === 0) { console.warn("No projects found. Check schema or RLS."); } } else if (view === 'developers') { const data = await DeveloperService.getAllDevelopers(); setItems(data); } } catch (err: any) { console.error("Fetch error:", err); setFetchError(err.message || "Unknown fetch error"); } setLoading(false); }; const handleForceSync = async () => { setSyncing(true); try { await SeedService.seedAll(); await fetchData(); alert("Base de données synchronisée avec succès !"); } catch (err: any) { alert("Erreur Sync: " + err.message); } setSyncing(false); }; const handleSave = async (e: React.FormEvent) => { e.preventDefault(); try { if (view === 'projects') { const payload = { id: editingItem.id || undefined, name: editingItem.name, developer_id: editingItem.developerId, status: editingItem.status || 'planning', type_asset: editingItem.projectType || 'apartment', address: editingItem.address, images: editingItem.images || [], location: { city: editingItem.city, district: editingItem.district }, prices: { ...editingItem.prices, sqm_observed: editingItem.prices?.avgSqm || 0 }, scores: { ...editingItem.audit?.trustScoreBreakdown, trust: editingItem.audit?.trustScore || 0 }, metadata: { ...editingItem.metadata, standing: editingItem.standing || 'moyen' } }; const { error: upsertErr } = await supabase .from('projects') .upsert(payload); if (upsertErr) throw upsertErr; } else if (view === 'developers') { const payload = { id: editingItem.id || undefined, name: editingItem.name, segment: editingItem.segment, scores: editingItem.scores, stats: editingItem.stats, verified: true }; const { error: upsertErr } = await supabase .from('developers') .upsert(payload); if (upsertErr) throw upsertErr; } setIsEditing(false); setEditingItem(null); fetchData(); } catch (err: any) { alert('Erreur lors de la sauvegarde: ' + err.message); } }; const handleDelete = async (id: string) => { if (!confirm('Voulez-vous vraiment supprimer cet élément ?')) return; const table = view === 'projects' ? 'projects' : 'developers'; const { error } = await supabase.from(table).delete().eq('id', id); if (error) { alert('Erreur suppression: ' + error.message); } else { fetchData(); } }; if (!isAuthenticated) { return ( <div className="min-h-screen bg-[#080808] flex items-center justify-center p-4"> <div className="max-w-md w-full bg-white/5 backdrop-blur-3xl border border-white/10 rounded-[3rem] p-12 space-y-8 animate-in fade-in zoom-in duration-700"> <div className="text-center space-y-2"> <div className="w-20 h-20 bg-primary/20 rounded-3xl flex items-center justify-center mx-auto mb-8 shadow-luxury-primary"> <Lock className="w-10 h-10 text-primary" /> </div> <h1 className="text-4xl font-black text-white uppercase italic tracking-tighter">Sovereign Access</h1> <p className="text-[10px] font-black text-slate-500 uppercase tracking-[0.3em] leading-relaxed">Avis Promoteur Maroc • Infrastructure Management</p> </div> <form onSubmit={handleLogin} className="space-y-6"> <div className="space-y-2"> <label className="text-[10px] font-black text-slate-400 uppercase tracking-widest ml-4">Clé de Déchiffrement</label> <input type="password" value={password} onChange={(e) => setPassword(e.target.value)} className="w-full bg-white/5 border border-white/10 rounded-2xl px-6 py-5 text-white focus:outline-none focus:border-primary/50 transition-all font-mono text-center tracking-widest" placeholder="••••••••" /> </div> {error && <p className="text-rose-500 text-[10px] font-black uppercase tracking-widest text-center animate-pulse">{error}</p>} <button type="submit" className="w-full bg-primary text-white py-6 rounded-2xl font-black text-[11px] uppercase tracking-[0.4em] shadow-2xl shadow-primary/30 hover:scale-[1.02] active:scale-95 transition-all"> Initialize Console </button> </form> </div> </div> ); } return ( <div className="min-h-screen bg-[#080808] text-white font-outfit"> {/* Sidebar Navigation */} <aside className="fixed left-0 top-0 bottom-0 w-80 bg-white/5 border-r border-white/10 p-10 flex flex-col justify-between z-50 backdrop-blur-xl"> <div className="space-y-12"> <div className="space-y-1"> <div className="w-12 h-1 bg-primary mb-4 rounded-full" /> <h2 className="text-3xl font-black italic text-primary uppercase leading-none tracking-tighter">Avis<br /><span className="text-white">Promoteur</span></h2> <p className="text-[9px] font-black text-slate-500 uppercase tracking-widest">Sovereign Intel Console v1.2</p> </div> <nav className="space-y-4"> {[ { id: 'projects', label: 'Projets Immobiliers', icon: Building2 }, { id: 'developers', label: 'Promoteurs', icon: ShieldCheck }, { id: 'reviews', label: 'Analyses & Flux', icon: TrendingUp } ].map(item => ( <button key={item.id} onClick={() => setView(item.id as any)} className={clsx( "w-full flex items-center gap-4 px-6 py-5 rounded-2xl transition-all font-black text-[10px] uppercase tracking-[0.2em]", view === item.id ? "bg-primary text-white shadow-luxury-primary" : "text-slate-500 hover:bg-white/5 hover:text-white" )} > <item.icon className="w-4 h-4" /> {item.label} </button> ))} </nav> </div> <div className="space-y-6"> <button onClick={handleForceSync} disabled={syncing} className="w-full flex items-center justify-center gap-4 bg-emerald-500/10 text-emerald-500 hover:bg-emerald-500 hover:text-white transition-all font-black text-[10px] uppercase tracking-[0.3em] py-5 border border-emerald-500/20 rounded-2xl disabled:opacity-50" > <RefreshCcw className={clsx("w-4 h-4", syncing && "animate-spin")} /> {syncing ? 'Syncing...' : 'Force DB Sync'} </button> <button onClick={() => setIsAuthenticated(false)} className="w-full flex items-center justify-center gap-4 text-slate-500 hover:text-rose-500 transition-all font-black text-[10px] uppercase tracking-[0.3em] py-4 border border-white/5 rounded-2xl"> <LogOut className="w-4 h-4" /> Terminate Session </button> </div> </aside> {/* Main Content Area */} <main className="pl-80 p-16"> <header className="flex justify-between items-end mb-20"> <div className="space-y-4"> <div className="flex items-center gap-3"> <div className="w-8 h-1 bg-primary rounded-full" /> <span className="text-[10px] font-black uppercase tracking-[0.5em] text-slate-500">Database Layer</span> </div> <h1 className="text-7xl font-black uppercase italic tracking-tighter leading-none"> Control <span className="text-primary">{view}</span> </h1> </div> <button onClick={() => { setIsEditing(true); setEditingItem({}); }} className="flex items-center gap-4 bg-white text-secondary px-10 py-5 rounded-3xl font-black text-[11px] uppercase tracking-[0.2em] hover:bg-primary hover:text-white transition-all shadow-2xl shadow-white/5 hover:scale-105" > <Plus className="w-5 h-5" /> Create New </button> </header> {/* Dynamic Table / Grid */} <div className="space-y-6"> {loading ? ( <div className="py-32 text-center animate-pulse flex flex-col items-center gap-6"> <div className="w-12 h-12 border-4 border-primary border-t-transparent rounded-full animate-spin" /> <div className="text-slate-500 font-black uppercase tracking-[0.5em] italic text-sm">Synchronizing Intelligence...</div> </div> ) : ( <div className="grid grid-cols-1 gap-6"> {items.map((p) => ( <div key={p.id} className="group bg-white/5 border border-white/5 rounded-[2.5rem] p-10 flex items-center justify-between hover:bg-white/10 hover:border-primary/30 transition-all duration-500"> <div className="flex items-center gap-10"> <div className="relative w-32 h-20 rounded-2xl overflow-hidden bg-slate-900 border border-white/5"> {(p.images?.[0] || p.avatar) && <img src={p.images?.[0] || p.avatar} alt={p.name} className="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-700" />} </div> <div className="space-y-2"> <h3 className="text-3xl font-black uppercase italic tracking-tighter group-hover:text-primary transition-colors">{p.name}</h3> <div className="flex items-center gap-6 text-[10px] font-black text-slate-500 uppercase tracking-[0.2em]"> <span className="flex items-center gap-2 text-white/40"><MapPin className="w-3.5 h-3.5" /> {p.city || p.segment}</span> <span className="w-1 h-1 bg-primary rounded-full" /> <span className="flex items-center gap-2"><ShieldCheck className="w-3.5 h-3.5 text-emerald-500" /> Score: {p.audit?.trustScore || p.scores?.reputation || 0}/10</span> </div> </div> </div> <div className="flex items-center gap-4 opacity-0 group-hover:opacity-100 transition-all duration-300 translate-x-4 group-hover:translate-x-0"> <button onClick={() => { setEditingItem(p); setIsEditing(true); }} className="p-5 bg-white/5 rounded-2xl hover:bg-primary hover:text-white transition-all shadow-xl" > <Edit2 className="w-5 h-5" /> </button> <button onClick={() => handleDelete(p.id)} className="p-5 bg-white/5 rounded-2xl hover:bg-rose-500 hover:text-white transition-all shadow-xl" > <Trash2 className="w-5 h-5" /> </button> </div> </div> ))} {fetchError && ( <div className="p-8 bg-rose-500/10 border border-rose-500/20 rounded-[2rem] text-rose-500 space-y-2"> <div className="flex items-center gap-3 font-black uppercase tracking-widest text-xs"> <AlertCircle className="w-4 h-4" /> Connectivity Error </div> <p className="font-mono text-sm">{fetchError}</p> </div> )} {items.length === 0 && !loading && !fetchError && ( <div className="py-32 text-center text-slate-600 font-black uppercase tracking-[0.4em] italic bg-white/5 rounded-[3rem] border border-dashed border-white/10 flex flex-col items-center gap-8"> <Database className="w-12 h-12 text-slate-800" /> <div>Empty Sector. Please run "Force DB Sync" to populate mock data or create items manually.</div> <div className="text-[9px] text-slate-500 not-italic tracking-normal">Tip: Ensure your Supabase tables match schema.sql and RLS is disabled or configured.</div> </div> )} </div> )} </div> </main> {/* Slide-over Edit Panel */} {isEditing && ( <div className="fixed inset-0 z-[100] flex justify-end"> <div className="absolute inset-0 bg-black/80 backdrop-blur-md" onClick={() => setIsEditing(false)} /> <div className="relative w-[700px] bg-[#0c0c0c] border-l border-white/10 h-full p-16 overflow-y-auto animate-in slide-in-from-right duration-700 shadow-[-50px_0_100px_rgba(0,0,0,0.5)]"> <header className="flex justify-between items-center mb-16"> <div className="space-y-2"> <div className="w-10 h-1 bg-primary rounded-full" /> <h2 className="text-4xl font-black uppercase italic tracking-tighter">Intelligence <span className="text-primary">Update</span></h2> </div> <button onClick={() => setIsEditing(false)} className="p-5 bg-white/5 rounded-full hover:bg-rose-500/20 hover:text-rose-500 transition-all"> <X className="w-6 h-6" /> </button> </header> <form onSubmit={handleSave} className="space-y-10"> <div className="space-y-8"> <div className="space-y-3"> <label className="text-[10px] font-black text-slate-500 uppercase tracking-[0.4em] ml-4">Primary Label</label> <input type="text" required value={editingItem?.name || ''} onChange={(e) => setEditingItem({...editingItem, name: e.target.value})} className="w-full bg-white/5 border border-white/10 rounded-[1.5rem] px-8 py-5 text-xl font-black uppercase italic text-white focus:outline-none focus:border-primary transition-all" placeholder="NOM DE L'ENTITÉ" /> </div> <div className="grid grid-cols-2 gap-8"> <div className="space-y-3"> <label className="text-[10px] font-black text-slate-500 uppercase tracking-[0.4em] ml-4">Location / Sector</label> <input type="text" value={editingItem?.city || editingItem?.segment || ''} onChange={(e) => view === 'projects' ? setEditingItem({...editingItem, city: e.target.value}) : setEditingItem({...editingItem, segment: e.target.value})} className="w-full bg-white/5 border border-white/10 rounded-[1.5rem] px-8 py-5 text-white focus:outline-none focus:border-primary transition-all font-bold" /> </div> <div className="space-y-3"> <label className="text-[10px] font-black text-slate-500 uppercase tracking-[0.4em] ml-4">Category / Status</label> {view === 'projects' ? ( <select value={editingItem?.standing || 'moyen'} onChange={(e) => setEditingItem({...editingItem, standing: e.target.value})} className="w-full bg-white/5 border border-white/10 rounded-[1.5rem] px-8 py-5 text-white focus:outline-none focus:border-primary transition-all font-bold" > <option value="economique">Économique</option> <option value="moyen">Moyen</option> <option value="haut">Haut</option> <option value="luxe">Luxe</option> </select> ) : ( <input type="text" value={editingItem?.developerType || ''} onChange={(e) => setEditingItem({...editingItem, developerType: e.target.value})} className="w-full bg-white/5 border border-white/10 rounded-[1.5rem] px-8 py-5 text-white focus:outline-none focus:border-primary transition-all font-bold" /> )} </div> </div> {view === 'projects' && ( <div className="space-y-3"> <label className="text-[10px] font-black text-slate-500 uppercase tracking-[0.4em] ml-4">Promoteur Rattaché</label> <select required value={editingItem?.developerId || ''} onChange={(e) => setEditingItem({...editingItem, developerId: e.target.value})} className="w-full bg-white/5 border border-white/10 rounded-[1.5rem] px-8 py-5 text-white focus:outline-none focus:border-primary transition-all font-bold" > <option value="">Sélectionner un promoteur...</option> {developers.map(dev => ( <option key={dev.id} value={dev.id}>{dev.name}</option> ))} </select> </div> )} <div className="space-y-3"> <label className="text-[10px] font-black text-slate-500 uppercase tracking-[0.4em] ml-4">Media Assets (URLs)</label> <textarea value={view === 'projects' ? (editingItem?.images?.join(', ') || '') : (editingItem?.avatar || '')} onChange={(e) => view === 'projects' ? setEditingItem({...editingItem, images: e.target.value.split(',').map(s => s.trim())}) : setEditingItem({...editingItem, avatar: e.target.value}) } className="w-full bg-white/5 border border-white/10 rounded-[1.5rem] px-8 py-5 text-white/60 focus:outline-none focus:border-primary transition-all h-32 font-mono text-xs" placeholder="https://..." /> </div> <div className="p-10 bg-primary/5 rounded-[2.5rem] border border-primary/20 space-y-6"> <div className="flex items-center gap-3 text-primary"> <ShieldCheck className="w-5 h-5" /> <span className="text-[10px] font-black uppercase tracking-[0.5em]">Sovereign Trust Rating</span> </div> <input type="range" min="0" max="10" step="0.1" value={view === 'projects' ? (editingItem?.audit?.trustScore || 0) : (editingItem?.scores?.reputation || 0)} onChange={(e) => view === 'projects' ? setEditingItem({ ...editingItem, audit: { ...editingItem.audit, trustScore: parseFloat(e.target.value) } }) : setEditingItem({ ...editingItem, scores: { ...editingItem.scores, reputation: parseFloat(e.target.value) } }) } className="w-full accent-primary h-2 rounded-full cursor-pointer" /> <div className="text-center text-5xl font-black italic text-white tabular-nums tracking-tighter"> {(view === 'projects' ? (editingItem?.audit?.trustScore || 0) : (editingItem?.scores?.reputation || 0)).toFixed(1)}<span className="text-xl text-slate-500 not-italic">/10</span> </div> </div> </div> <button type="submit" className="w-full bg-primary text-white py-7 rounded-[2rem] font-black text-[12px] uppercase tracking-[0.5em] shadow-2xl shadow-primary/30 flex items-center justify-center gap-4 hover:bg-white hover:text-secondary transition-all duration-500 transform hover:translate-y-[-4px]"> <Save className="w-5 h-5" /> Commit to Database </button> </form> </div> </div> )} </div> );
} function clsx(...classes: any[]) { return classes.filter(Boolean).join(' ');
}
