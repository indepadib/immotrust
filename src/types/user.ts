import { Project, Developer, ImmoReview } from './immo'; export interface User { id: string; email: string; name: string; role: 'user' | 'expert' | 'admin'; avatar?: string; karma: number; badge?: string; verifiedAt?: string; } export interface ExpertProfile extends User { specialty: string[]; reviewsCount: number; trustScoreContribution: number; bio?: string; } 