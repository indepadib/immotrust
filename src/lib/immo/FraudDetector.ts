import { ImmoReview } from '@/types/immo'; export class FraudDetector { /** * AI-driven linguistic pattern matcher to detect shills and manufactured reviews. * Flawless & Intelligent sovereign logic. */ static analyzeReview(review: ImmoReview): { isSuspicious: boolean, score: number, reason?: string } { const text = (review.body || '').toLowerCase(); let riskScore = 0; const reasons: string[] = []; // 1. Shilling Patterns (Hyperbolic praise without verification) const hyperbolicPraise = ['incroyable', 'parfait', 'meilleur', 'magnifique', 'absolument', 'génial', 'must']; const praiseCount = hyperbolicPraise.filter(p => text.includes(p)).length; if (praiseCount >= 2 && review.ratingOverall >= 9 && !review.purchaseVerified) { riskScore += 40; reasons.push('Praise Hyperbolique (Haut Risque)'); } // 2. Short/Low Density if (text.length > 0 && text.length < 30) { riskScore += 30; reasons.push('Contenu trop succinct'); } // 3. Automated Pattern (Repetition) const words = text.split(' '); const uniqueWords = new Set(words); if (words.length > 8 && (uniqueWords.size / words.length) < 0.5) { riskScore += 50; reasons.push('Pattern de répétition suspect'); } // 4. Spam Detection (External Links) const urlPattern = /(https?:\/\/[^\s]+)/g; if (urlPattern.test(text)) { riskScore += 80; reasons.push('Liens externes détectés (Spam)'); } return { isSuspicious: riskScore >= 50, score: riskScore, reason: reasons.join(' • ') || undefined }; } } 